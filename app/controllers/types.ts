export type movieType = {
  id: string;
  title: string;
  release_date: string;
  director: string;
  producer: string;
};

export type swapiFilmsDTO = {
  episode_id: string;
  title: string;
  release_date: string;
  director: string;
  producer: string;
};
